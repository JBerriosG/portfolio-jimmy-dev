---
import { Trans } from "astro-i18next/components";
interface Props {
  url: string;
  alt: string;
}

const { url, alt } = Astro.props;
---

<section
  class="flex justify-center py-4 text-pretty text-left text-2xl md:text-4xl pt-32 px-4 md:px-2"
>
  <div class="flex w-full max-w-[1000px] justify-between items-center">
    <p class="font-bold">
      <Trans i18nKey="intro.parrafo">
        Hola 👋, <br> Mi nombre es <span
          class="bg-gradient-to-r from-blue-600 via-green-500 to-indigo-400 inline-block text-transparent bg-clip-text"
          >Jimmy Berrios Gallo.
        </span><br> Construyo webs y software a la medida.
      </Trans>
    </p>
    <img
      src={url}
      alt={alt}
      class="w-36 md:w-52 rounded-full border-4 bg-gradient-to-r from-blue-600 via-green-500 to-indigo-400 border-transparent"
    />
  </div>
</section>
